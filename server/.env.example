# Example env file for development
# Copy this to .env and edit values as needed before running the server

PORT=5000
MONGODB_URI=mongodb://localhost:27017/gaji-demara
NODE_ENV=development
JWT_SECRET=change_this_before_production
# Optional: key required to register admin accounts
ADMIN_REGISTER_KEY=change_this_to_secret_key
